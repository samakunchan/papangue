import { ParsePortfolioPipe } from './parse-portfolio.pipe';

describe('ParsePortfolioPipe', () => {
  it('create an instance', () => {
    const pipe = new ParsePortfolioPipe();
    expect(pipe).toBeTruthy();
  });
});

import { GroupCategoryPipe } from './group-category.pipe';

describe('GroupCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
